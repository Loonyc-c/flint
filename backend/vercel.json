{
  "version": 2,
  "buildCommand": "npm run copy:shared",
  "installCommand": "npm install && cd ../shared && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ],
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  }
}
