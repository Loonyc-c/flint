{
  "version": 2,
  "framework": null,
  "installCommand": "npm install --prefix shared && npm install --prefix frontend && npm install --prefix backend",
  "buildCommand": "cd frontend && npm run copy:shared && npm run build",
  "outputDirectory": "frontend/.next",
  "regions": ["sfo1"],
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/v1/:path*",
      "destination": "/api/v1/:path*"
    }
  ],
  "functions": {
    "api/v1/[...].ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  }
}
