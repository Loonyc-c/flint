{
  "version": 2,
  "installCommand": "ls -la && npm install --prefix shared && npm install --prefix frontend && npm install --prefix backend",
  "buildCommand": "cd frontend && npm run copy:shared && npm run build",
  "framework": "nextjs",
  "outputDirectory": "frontend/.next",
  "regions": ["sfo1"],
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/v1/:path*",
      "destination": "/api/v1/:path*"
    }
  ],
  "functions": {
    "api/v1/[...].ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  }
}
