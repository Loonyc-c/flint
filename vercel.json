{
  "version": 2,
  "installCommand": "npm install && npm install --prefix shared && npm install --prefix frontend && npm install --prefix backend",
  "buildCommand": "cd backend && npm run build && cd ../frontend && npm run copy:shared && npm run build && cp -r ../backend/dist .next/server/backend-dist",
  "outputDirectory": "frontend/.next"
}
